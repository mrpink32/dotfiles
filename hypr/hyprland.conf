
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

exec-once=dunst & waybar & hyprpaper

# autogenerated = 0 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
monitor=eDP-1, 3840x2160@120, 0x0, 1.0
monitor=DP-1, 3840x1100@60, 0x2160, 1.5
monitor=DP-2, 3840x1100@60, 0x2160, 1.5
monitor=HDMI-A-1, 1920x1080@60, 3840x0, 1, transform,1
# monitor=HDMI-A-1, preffered, auto, 1 #1920x1080@60, 3840x0, 1, transform,1



# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24
env = WLR_DRM_NO_ATOMIC,1

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
# See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 0
    inactive_opacity = 0.9
    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    blur {
        enabled = true
        size = 8
        passes = 1
        new_optimizations = true
    }
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,dk
    kb_variant = dvorak,dvorak
    kb_model =
    kb_options = caps:backspace, shift:both_capslock, grp:win_space_toggle
    kb_rules =

    repeat_rate = 75
    repeat_delay = 200

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    accel_profile = flat

    natural_scroll = false

    follow_mouse = 2

    touchpad {
        natural_scroll = false
    }

    touchdevice {

    }
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = true
    workspace_swipe_fingers = 3
    workspace_swipe_use_r = false

}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, alacritty
bind = $mainMod, C, killactive, 
bind = $mainMod, M, exit, 
bind = $mainMod, E, exec, dolphin
bind = $mainMod, V, togglefloating, 
bindr = $mainMod, R, exec, fuzzel || pkill fuzzel
bind = $mainMod, F, fullscreen
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, U, togglesplit, # dwindle


# Volume
bindle=, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+
bindle=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindle=, XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Media controls
bindl=, XF86AudioPlay, exec, playerctl play-pause
bindl=, XF86AudioNext, exec, playerctl next
bindl=, XF86AudioPrev, exec, playerctl previous
bindl=, XF86AudioStop, exec, playerctl stop
bindl=, XF86AudioPause, exec, playerctl pause

# use wpctl for wireplumber support
# or pw-cli, pw-mon & pw-top instead of pactl
# pactl set-default-sink alsa_output.usb-ASUSTekcomputer.Inc_USB_Audio_202101110002-00.analog-stereo
#
bindle=,XF86MonBrightnessDown,exec,brightnessctl set 5%-
bindle=,XF86MonBrightnessUp,exec,brightnessctl set +5%

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r

bind = $mainMod, n, cyclenext
# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, H, movewindow, l

# Switch between monitor scales
bindl = $mainMod, X, exec, hyprctl keyword monitor eDP-1,3840x2160@120,0x0,2.0
bindl = $mainMod, K, exec, hyprctl keyword monitor eDP-1,3840x2160@120,0x0,1.5
bindl = $mainMod, J, exec, hyprctl keyword monitor eDP-1,3840x2160@120,0x0,1.0

# Toggle secondary display
bindl = $mainMod CTRL SHIFT, 1, exec, hyprctl keyword monitor DP-2, disable
bindl = $mainMod CTRL SHIFT, 2, exec, hyprctl keyword monitor DP-2,3840x1100@60, 0x2160, 1.5
bindl = $mainMod CTRL SHIFT, 1, exec, hyprctl keyword monitor DP-1, disable
bindl = $mainMod CTRL SHIFT, 2, exec, hyprctl keyword monitor DP-1,3840x1100@60, 0x2160, 1.5


# bindl = $mainMod CTRL SHIFT, 3, exec, hyprctl keyword monitor DP-2,3840x1100@60, 0x2160, 1.5 || hyprctl keyword monitor DP-2, disable


# figure out how to toggle monitor stuff and enable ability to mirror hdmi or support external screeen at home
# bindl = $mainMod,T,exec, hyprctl keyword monitor "DP-2,3840x1100@60, 0x2160, 1.5" || hyprctl keyword monitor "DP-2, disable"


misc {
    disable_hyprland_logo = true
    disable_splash_rendering = false
    # disable_hypr_chan = false
    force_hypr_chan = true
    vfr = true
    vrr = 2
    mouse_move_enables_dpms = false
    key_press_enables_dpms = false
    always_follow_on_dnd = true
}

